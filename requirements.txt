# ──────────────────────────────────────────────────────────
#  AWAIS X HACKER TEAM — WPA2 AUDIT ENGINE v5.0
# ──────────────────────────────────────────────────────────
#  Python dependencies: NONE (all standard library).
#
#  Optional SYSTEM packages (for real .cap cracking):
#
#    aircrack-ng   →  primary .cap cracking engine
#                     Termux : pkg install aircrack-ng
#                     Debian : sudo apt install aircrack-ng
#                     macOS  : brew install aircrack-ng
#
#    hashcat       →  faster GPU/CPU cracking (optional)
#                     Termux : pkg install hashcat
#                     Debian : sudo apt install hashcat
#                     macOS  : brew install hashcat
#
#    hcxtools      →  provides cap2hccapx (for hashcat mode)
#                     Termux : pkg install hcxtools
#                     Debian : sudo apt install hcxtools
#                     macOS  : brew install hcxtools
#
#  Python version: 3.7 or higher
# ──────────────────────────────────────────────────────────